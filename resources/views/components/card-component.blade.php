<div class="bg-gradient-to-r from-{{ $colorGradient }}-300 to-{{ $colorBg }}-300 rounded-2xl p-5 border-4 border-black shadow-lg transform -rotate-1">
    <div class="flex justify-between items-center">
        <span class="text-gray-900 font-black text-lg"> {{ $title }}</span>
        <span class="text-4xl font-black text-{{ $colorText }}-900">{{ $data }}</span>
    </div>
</div>